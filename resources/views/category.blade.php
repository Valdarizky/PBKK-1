{{-- @extends('template')

@section('container')
   <h2> Post Category : {{ $category }}</h2> --}}
   {{-- <h5>editor: {{ $name }} | {{ $email }}</h5> --}}
   {{-- <hr/>
    @foreach($articles as $article)
        <article class="mb-5">
            <h3> 
                <a href="/article/{{ $article->slug }}" class="text-decpration-none">
                {{ $article->title }} 
                </a>                    
            </h3>
            <h6>By: {{ $article->author->name}}</h6>
            <p>{!! $article->excerpt!!}</p>
        </article>
    @endforeach
@endsection --}}
